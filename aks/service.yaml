apiVersion: v1
kind: Service
metadata:
  name: fabric-data-agent
  namespace: fabric-data-agent
spec:
  type: ClusterIP
  selector:
    app: fabric-data-agent
  ports:
    - port: 80
      targetPort: 8501
      name: http
